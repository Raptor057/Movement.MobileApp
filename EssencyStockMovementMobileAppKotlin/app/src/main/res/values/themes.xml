<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.EssencyStockMovement" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorOnPrimary">@android:color/black</item>

        <!-- Fondo claro -->
        <item name="android:windowBackground">@android:color/white</item>

        <!-- Texto negro -->
        <item name="colorOnSurface">@android:color/black</item>
        <item name="colorOnBackground">@android:color/black</item>

        <!-- Hint gris -->
        <item name="android:textColorHint">@android:color/darker_gray</item>

        <!-- Campos de texto -->
        <item name="colorControlNormal">@android:color/black</item>
        <item name="colorControlActivated">@android:color/black</item>
    </style>

    <!-- Requeridos para el AppBar o diálogos si los usas -->
    <style name="Theme.EssencyStockMovement.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Light" />
    <style name="Theme.EssencyStockMovement.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />
</resources>
